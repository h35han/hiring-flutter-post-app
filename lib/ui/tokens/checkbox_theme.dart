import 'package:flutter/material.dart';

import 'colors.dart';

final appCheckboxThemeData = CheckboxThemeData(
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(5)),
  side: BorderSide(color: AppColors.line),
);
